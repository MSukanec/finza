import { RecurrentesView } from '@/features/recurrentes/views/recurrentes-view';

export const metadata = {
  title: 'Gastos Recurrentes | Finza',
};

export default function RecurrentesPage() {
  return <RecurrentesView />;
}
